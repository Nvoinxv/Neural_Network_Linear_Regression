#include "optimisasi_neuron.hpp"

Optimisasi_Adam::Optimisasi_Adam(): 
Linear(), Mean_Squared_Error(), beta_1(0.0f),
beta_2(0.0f), w(), v(), alpha(0.0f), epsilon(0.0f) {
    // kosong
}


std::vector<float> Optimisasi_Adam::momentum_gradient_m() {
    for (int i )
}